pb_add_primitive_fx_plugin(
  pbPFL_Distortion
  "pbPFL Distortion"
  PrDs
  "Distortion"
  "Drive processor with selectable soft saturation, tone shaping, bias, mix, and output control."
  "${CMAKE_CURRENT_SOURCE_DIR}/mock"
)
