pb_add_primitive_fx_plugin(
  pbPFL_AutoPan
  "pbPFL AutoPan"
  PrAP
  "AutoPan"
  "Tempo-sync capable auto panner with rate, depth, phase, shape, offset, Dry/Wet, and output trim."
  "${CMAKE_CURRENT_SOURCE_DIR}/mock"
)
