pb_add_primitive_fx_plugin(
  pbPFL_PitchShifter
  "pbPFL PitchShifter"
  PrPs
  "PitchShifter"
  "Time-domain pitch shifter with semitone, cent, grain size, crossfade, formant tone, Dry/Wet, and output trim."
  "${CMAKE_CURRENT_SOURCE_DIR}/mock"
)
